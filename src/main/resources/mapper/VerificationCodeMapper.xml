<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.priceplatform.dao.VerificationCodeMapper">

    <!-- 插入验证码 -->
    <insert id="insert" parameterType="com.priceplatform.entity.VerificationCode"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO verification_code (
            account, code, type, expire_time, is_used
        ) VALUES (
                     #{account}, #{code}, #{type}, #{expireTime}, #{isUsed}
                 )
    </insert>

    <!-- 根据账号和类型查询最新的验证码 -->
    <select id="selectLatestByAccountAndType" resultType="com.priceplatform.entity.VerificationCode">
        SELECT
            id,
            account,
            code,
            type,
            expire_time as expireTime,
            is_used as isUsed,
            create_time as createTime
        FROM verification_code
        WHERE account = #{account}
          AND type = #{type}
        ORDER BY create_time DESC
            LIMIT 1
    </select>

    <!-- 更新验证码使用状态 -->
    <update id="updateIsUsed">
        UPDATE verification_code
        SET is_used = #{isUsed}
        WHERE id = #{id}
    </update>

</mapper>