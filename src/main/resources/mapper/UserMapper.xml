<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.todayeatapp.auth.dao.UserMapper">

    <!-- 检查手机号是否已存在 -->
    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(1) > 0 FROM user WHERE phone = #{phone}
    </select>

    <!-- 检查邮箱是否已存在 -->
    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0 FROM user WHERE email = #{email}
    </select>

    <!-- 插入新用户 -->
    <insert id="insert" parameterType="com.todayeatapp.auth.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (phone, email, password, nickname, avatar, status, last_login_time)
        VALUES (#{phone}, #{email}, #{password}, #{nickname}, #{avatar}, #{status}, #{lastLoginTime})
    </insert>

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultType="com.todayeatapp.auth.entity.User">
        SELECT id, phone, email, password, nickname, avatar, status,
               last_login_time as lastLoginTime,
               create_time as createTime,
               update_time as updateTime
        FROM user WHERE id = #{id}
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="selectByPhone" resultType="com.todayeatapp.auth.entity.User">
        SELECT id, phone, email, password, nickname, avatar, status,
               last_login_time as lastLoginTime,
               create_time as createTime,
               update_time as updateTime
        FROM user WHERE phone = #{phone}
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="selectByEmail" resultType="com.todayeatapp.auth.entity.User">
        SELECT id, phone, email, password, nickname, avatar, status,
               last_login_time as lastLoginTime,
               create_time as createTime,
               update_time as updateTime
        FROM user WHERE email = #{email}
    </select>

    <!-- 根据手机号或邮箱查询用户（用于登录） -->
    <select id="selectByPhoneOrEmail" resultType="com.todayeatapp.auth.entity.User">
        SELECT id, phone, email, password, nickname, avatar, status,
               last_login_time as lastLoginTime,
               create_time as createTime,
               update_time as updateTime
        FROM user WHERE phone = #{account} OR email = #{account}
    </select>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="com.todayeatapp.auth.entity.User">
        UPDATE user
        <set>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="password != null">password = #{password},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新最后登录时间 -->
    <update id="updateLastLoginTime">
        UPDATE user
        SET last_login_time = #{lastLoginTime}
        WHERE id = #{id}
    </update>

</mapper>