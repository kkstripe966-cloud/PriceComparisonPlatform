server:
  port: 8081
  servlet:
    context-path: /priceplatform

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/price_comparison?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: wff13377072928
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20

mybatis:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.priceplatform.entity

logging:
  level:
    com.priceplatform: DEBUG
    org.springframework: INFO

jwt:
  secret: price_platform_secret_key_2023
  expire: 604800
  header: Authorization
  token-start: Bearer

mail:
  host: smtp.qq.com
  port: 587
  username: 2313751423@qq.com
  password: brjzskidvkrvdhie
  protocol: smtp
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
          required: true
        from: 2313751423@qq.com
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000